{
  "name": "ray-tracing-ts-server",
  "version": "1.0.0",
  "module": "build/index.js",
  "devDependencies": {
    "@types/node": "^18.6.4",
    "@types/ws": "^8.5.3"
  },
  "scripts": {
    "build": "tsc",
    "start": "bun start:node",
    "start:node": "tsc && node ./build/index.js",
    "start:bun": "bun run src/index.ts",
    "benchmark:bun": "bun run ./src/benchmark.ts",
    "benchmark:node": "bun build && node ./build/benchmark.js"
  },
  "dependencies": {
    "ws": "^8.8.1"
  }
}